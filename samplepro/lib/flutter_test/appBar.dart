appBar: new AppBar(
          title: new Text("khalefa"),
          backgroundColor: Colors.green,
          actions: [
            IconButton(
                icon: const Icon(Icons.accessible_forward_sharp),
                onPressed: onClick)
          ],
        ),